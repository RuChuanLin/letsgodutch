import { Layout } from "antd";
import { usingLocalDB } from "../../firebase/init";
const { Footer } = Layout;

const StyledFooter = () => {
  return <Footer>32</Footer>;
};

export default StyledFooter;
